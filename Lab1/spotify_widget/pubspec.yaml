name: spotify_widget
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  # This line appears ONLY ONCE now:
  uses-material-design: true

  # Your assets configuration:
  assets:
    - assets/image_640283.jpeg